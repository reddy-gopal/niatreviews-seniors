# Static assets

Place your **website logo** here so it appears in the navbar.

- **Recommended:** `logo.png` or `logo.svg`
- The Navbar uses `/logo.png` by default. To use a different name, update `Navbar.tsx` (search for `logo.png`).

Supported formats: PNG, SVG, WebP, JPG.
