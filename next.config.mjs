/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Next 15+ expects object; empty object satisfies config (indicator may still show in dev)
  devIndicators: {},
};

export default nextConfig;
